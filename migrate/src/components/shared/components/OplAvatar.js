import { Avatar } from '@material-ui/core';
import { sizing, palette } from "@material-ui/system";
import styled from "styled-components";

const OplAvatar = styled(Avatar)`${sizing}${palette}`;

export default OplAvatar;
